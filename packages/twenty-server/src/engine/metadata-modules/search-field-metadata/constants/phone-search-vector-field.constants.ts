import { msg } from '@lingui/core/macro';

export const PHONE_SEARCH_VECTOR_FIELD = {
  name: 'phoneSearchVector',
  label: msg`Phone search vector`,
  description: msg`Field used for exact phone search`,
} as const;
